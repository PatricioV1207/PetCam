# PetCam environment configuration
# Copy to /opt/petcam/.env and adjust values.

DEVICE=/dev/video0

# V4L2 input format used by FFmpeg.
# Common values: mjpeg, yuyv422
# To find what your webcam supports, run:
#   v4l2-ctl -d /dev/video0 --list-formats-ext
# If mjpeg is not available, change this to yuyv422.
INPUT_FORMAT=mjpeg

# MVP settings: 720p30 at 2 Mbps for stability.
# These can be increased later if the Pi 5 handles it well.
RESOLUTION=1280x720
FRAMERATE=30
BITRATE=2M
PRESET=veryfast

MEDIAMTX_HOST=localhost
MEDIAMTX_PORT=8554
STREAM_PATH=cam
